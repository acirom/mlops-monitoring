apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: custom-grafana
  namespace: openshift-gitops
spec:
  destination:
    namespace: my-grafana
    server: https://kubernetes.default.svc
  project: default
  source:
    path: grafana/gitops/overlays/dashboards
    repoURL: https://github.com/acirom/mlops-monitoring.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
